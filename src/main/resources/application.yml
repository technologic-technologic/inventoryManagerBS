server:
  port: 9090

spring:
  application:
    name: inventory-manager-spark

  datasource:
    url: jdbc:h2:mem:testdb
    username: 'sa'
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
